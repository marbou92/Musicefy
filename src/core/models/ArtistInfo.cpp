// ArtistInfo.cpp

#include "ArtistInfo.h"
#include "AlbumInfo.h"

// The ArtistInfo getter/setter for albums() is defined in AlbumInfo.cpp
// because of the forward-decl pattern. Keep this file as the home for any
// ArtistInfo member functions that don't need AlbumInfo's full definition.

namespace mf::core::models {
// (no out-of-line definitions needed beyond the cross-referenced albums())
}
