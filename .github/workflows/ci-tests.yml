name: CI - Build & Test (Matrix)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: windows-2022
    strategy:
      matrix:
        configuration: [ Debug, Release ]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v3
        with:
          version: '5.15.2'
          host: 'windows'
          target: 'desktop'
          arch: 'win64_msvc2019_64'

      - name: Configure CMake
        shell: cmd
        run: |
          call scripts\configure-msvc.bat ${{ matrix.configuration }} "${{ env.QT_PATH }}"

      - name: Build Project
        shell: cmd
        run: |
          cmake --build build --config ${{ matrix.configuration }} --parallel

      - name: Run CTest unit tests
        shell: cmd
        run: |
          cd build
          ctest --output-on-failure -C ${{ matrix.configuration }}

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.configuration }}
          path: build/Testing/Temporary/
