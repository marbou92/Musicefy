<Window x:Class="Musicefy.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" ResizeMode="NoResize"
        Width="420" Height="420"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False" AllowsTransparency="True"
        Opacity="0"
        Background="Transparent">

    <Border x:Name="MainRootBorder" CornerRadius="28" Background="#0A0A0A" RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <TransformGroup>
                <ScaleTransform x:Name="RootScale" ScaleX="0.95" ScaleY="0.95"/>
            </TransformGroup>
        </Border.RenderTransform>
        
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="50" ShadowDepth="0" Opacity="0.75"/>
        </Border.Effect>

        <Grid>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                
                <Grid Width="110" Height="110" Margin="0,0,0,20" RenderTransformOrigin="0.5,0.5">
                    <Grid.RenderTransform>
                        <ScaleTransform x:Name="LogoScale" ScaleX="1.0" ScaleY="1.0"/>
                    </Grid.RenderTransform>
                    
                    <Image Source="Assets/musicefy.png" RenderOptions.BitmapScalingMode="HighQuality">
                        <Image.Effect>
                            <DropShadowEffect Color="#1DB954" BlurRadius="25" ShadowDepth="0" Opacity="0.25"/>
                        </Image.Effect>
                    </Image>
                </Grid>

                <TextBlock Text="Musicefy" FontSize="36" FontWeight="Bold" 
                           Foreground="White" HorizontalAlignment="Center"
                           FontFamily="Segoe UI" LetterSpacing="1">
                    <TextBlock.Effect>
                        <DropShadowEffect BlurRadius="8" Opacity="0.3" ShadowDepth="2" Color="Black"/>
                    </TextBlock.Effect>
                </TextBlock>

                <TextBlock Text="INITIALIZING AUDIO ENGINE" FontSize="10" FontWeight="Bold" 
                           Foreground="#6A6A6A" Margin="0,16,0,0" CharacterSpacing="150"
                           HorizontalAlignment="Center"/>
            </StackPanel>

            <Grid VerticalAlignment="Bottom" Margin="48,0,48,44">
                <Border Height="3" Background="#1A1A1A" CornerRadius="1.5"/>
                
                <ProgressBar x:Name="BottomProgress" Height="3" 
                             Background="Transparent" BorderThickness="0" Foreground="#1DB954"
                             Value="0" Maximum="100"/>
            </Grid>
        </Grid>
    </Border>
</Window>
