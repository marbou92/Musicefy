<Window x:Class="Musicefy.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Height="600" Width="900"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}">

    <Window.Resources>
        <!-- Sidebar ToggleButton Style -->
        <Style x:Key="SidebarButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentHoverBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentPressedBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentPressedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/> <!-- Sidebar -->
            <ColumnDefinition Width="*"/>   <!-- Content -->
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <StackPanel Grid.Column="0" Background="{DynamicResource SecondaryBackgroundBrush}">
            <TextBlock Text="Settings" FontSize="24" FontWeight="Bold" Margin="20"/>

            <ToggleButton x:Name="AppearanceButton"
                          Style="{StaticResource SidebarButtonStyle}" 
                          Margin="0,0,0,10"
                          IsChecked="True"
                          Click="AppearanceButton_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🎨" FontSize="16" Margin="0,0,8,0"/>
                    <StackPanel>
                        <TextBlock Text="Appearance" FontWeight="Bold"/>
                        <TextBlock Text="Theme &amp; palettes" FontSize="12"/>
                    </StackPanel>
                </StackPanel>
            </ToggleButton>

            <ToggleButton x:Name="DownloadsButton"
                          Style="{StaticResource SidebarButtonStyle}" 
                          Margin="0,0,0,10"
                          Click="DownloadsButton_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⬇️" FontSize="16" Margin="0,0,8,0"/>
                    <StackPanel>
                        <TextBlock Text="Downloads" FontWeight="Bold"/>
                        <TextBlock Text="Cache &amp; limits" FontSize="12"/>
                    </StackPanel>
                </StackPanel>
            </ToggleButton>

            <ToggleButton x:Name="SourcesButton"
                          Style="{StaticResource SidebarButtonStyle}" 
                          Click="SourcesButton_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔗" FontSize="16" Margin="0,0,8,0"/>
                    <StackPanel>
                        <TextBlock Text="Sources" FontWeight="Bold"/>
                        <TextBlock Text="Streaming connections" FontSize="12"/>
                    </StackPanel>
                </StackPanel>
            </ToggleButton>
        </StackPanel>

        <!-- Main Content -->
        <Grid Grid.Column="1" Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0" x:Name="SectionTitle"
                       Text="Appearance Settings" 
                       FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

            <!-- Appearance Settings -->
            <StackPanel Grid.Row="1" Orientation="Vertical">
                    <TextBlock Text="Theme Mode" FontWeight="Bold" Margin="0,0,0,5"/>
                    <ComboBox x:Name="ModeCombo" Width="200" SelectedIndex="1" Margin="0,0,0,10">
                        <ComboBoxItem Content="System"/>
                        <ComboBoxItem Content="Light"/>
                        <ComboBoxItem Content="Dark"/>
                        <ComboBoxItem Content="DarkPure"/>
                    </ComboBox>
                
                    <TextBlock Text="Palette" FontWeight="Bold" Margin="0,12,0,0"/>
                    <ComboBox x:Name="PaletteCombo" Width="200" SelectedIndex="0" Margin="0,0,0,10">
                        <ComboBoxItem Content="Default"/>
                        <ComboBoxItem Content="Catppuccin"/>
                        <ComboBoxItem Content="GreenApple"/>
                        <ComboBoxItem Content="Lavender"/>
                    </ComboBox>
                </StackPanel>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Cancel" Width="100" Margin="0,0,10,0" Style="{StaticResource PrimaryButtonStyle}" Click="Cancel_Click"/>
                <Button Content="Save" Width="100" Style="{StaticResource PrimaryButtonStyle}" Click="Save_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
