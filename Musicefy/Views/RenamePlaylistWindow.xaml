<Window x:Class="Musicefy.Views.RenamePlaylistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename Playlist" Height="180" Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource SurfaceBrush}"
        Foreground="{DynamicResource OnSurfaceBrush}"
        TextElement.FontFamily="Segoe UI">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Playlist Name" FontSize="14" FontWeight="SemiBold"
                   Foreground="{DynamicResource OnSurfaceBrush}" Margin="0,0,0,8"/>

        <TextBox x:Name="TxtPlaylistName" Grid.Row="1"
                 FontSize="14" Padding="8,6"
                 Background="{DynamicResource SurfaceContainerHighBrush}"
                 Foreground="{DynamicResource OnSurfaceBrush}"
                 BorderBrush="{DynamicResource OutlineBrush}"
                 BorderThickness="1"
                 CaretBrush="{DynamicResource OnSurfaceBrush}"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Width="80" Height="32" Margin="0,0,12,0"
                    Background="Transparent" Foreground="{DynamicResource OnSurfaceBrush}"
                    BorderBrush="{DynamicResource OutlineBrush}" BorderThickness="1"
                    IsCancel="True" Click="BtnCancel_Click"/>
            <Button Content="Rename" Width="80" Height="32"
                    Background="{DynamicResource PrimaryBrush}" Foreground="{DynamicResource OnPrimaryBrush}"
                    BorderThickness="0" Cursor="Hand"
                    IsDefault="True" Click="BtnRename_Click"/>
        </StackPanel>
    </Grid>
</Window>
