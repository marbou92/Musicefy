<UserControl x:Class="Musicefy.Views.SourcesSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10,0,10,10">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Orientation="Vertical">

                <TextBlock Text="Streaming Connections" FontSize="22" FontWeight="Bold" Margin="4,0,0,4" Foreground="{DynamicResource ForegroundBrush}"/>
                <TextBlock Text="Add and manage remote music sources." Foreground="{DynamicResource MutedTextBrush}" Margin="4,0,0,20" FontSize="12"/>

                <Border Background="{DynamicResource SecondaryBackgroundBrush}" CornerRadius="12" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Add New Source" FontWeight="Bold" FontSize="13" Margin="0,0,0,12" Foreground="{DynamicResource ForegroundBrush}"/>

                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="Source Type" FontWeight="Bold" Margin="0,0,0,6" FontSize="13" Foreground="{DynamicResource ForegroundBrush}"/>
                            <ComboBox x:Name="SourceTypeCombo" DisplayMemberPath="DisplayName" SelectionChanged="SourceTypeCombo_SelectionChanged"/>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="Connection Name" FontWeight="Bold" Margin="0,0,0,6" FontSize="13" Foreground="{DynamicResource ForegroundBrush}"/>
                            <Border Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Height="38">
                                <TextBox x:Name="NameTextBox" VerticalAlignment="Center"/>
                            </Border>
                        </StackPanel>

                        <StackPanel x:Name="DynamicFieldsStack" Margin="0,0,0,12"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,4,0,0">
                            <TextBlock x:Name="TestStatus" Visibility="Collapsed" FontSize="12" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <Button x:Name="TestButton" Content="Test Connection" Width="140" Margin="0,0,10,0" Click="TestButton_Click"/>
                            <Button x:Name="AddButton" Content="Link Service" Width="140" Click="AddButton_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Background="{DynamicResource SecondaryBackgroundBrush}" CornerRadius="12" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Connected Sources" FontWeight="Bold" FontSize="13" Margin="0,0,0,12" Foreground="{DynamicResource ForegroundBrush}"/>
                        <ItemsControl x:Name="SourcesList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource BackgroundBrush}" CornerRadius="10" Padding="12" Margin="0,4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <Border Grid.Column="0" Width="36" Height="36" CornerRadius="18"
                                                    Background="{DynamicResource AccentBrush}" Margin="0,0,12,0">
                                                <Path Data="{Binding Type, Converter={StaticResource IconGlyphConverter}}"
                                                      Width="18" Height="18" Stretch="Uniform"
                                                      Fill="White"
                                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>

                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource ForegroundBrush}"/>
                                                <TextBlock Text="{Binding DisplayType}" FontSize="11" Foreground="{DynamicResource MutedTextBrush}"/>
                                            </StackPanel>

                                            <Button Grid.Column="2" Content="Remove" Click="RemoveSource_Click"
                                                    Tag="{Binding Id}" Foreground="#e74c3c" VerticalAlignment="Center"
                                                    Background="Transparent" FontSize="12"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
